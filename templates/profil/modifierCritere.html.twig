{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        {{ form_start(modifForm) }}
        {{ form_widget(modifForm) }}
        {{ form_end(modifForm) }}
    </div>

{% endblock %}

{% block title %}

{% endblock %}

